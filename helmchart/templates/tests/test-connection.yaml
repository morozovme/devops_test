apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gomainchart.fullname" . }}-test-connection"
  labels:
    {{- include "gomainchart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gomainchart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
