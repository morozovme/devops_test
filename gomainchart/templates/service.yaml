apiVersion: v1
kind: Service
metadata:
  name: {{ include "gomainchart.fullname" . }}
  labels:
    {{- include "gomainchart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gomainchart.selectorLabels" . | nindent 4 }}
